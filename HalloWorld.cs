using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playermovmant : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("SVETLO SVIETI!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
